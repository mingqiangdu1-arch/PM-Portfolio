"use client";

import { FormEvent, useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { StatusPanel } from "@/components/status-panel";
import { frontendApi } from "@/lib/api/frontend-api";
import type { ConfirmationRoundView, FrontendApi, ImplementationPlanView, ProjectCapabilities, ReadinessView, Scenario } from "@/lib/api/ports";

const emptyReadiness: ReadinessView = { schemaVersion: "implementation_confirmation.readiness.mvp3.v1", scopeStatus: "not_ready", implementationStatus: "not_ready", configurationStatus: "not_applicable", dataChangeStatus: "not_applicable", knownBlockers: [] };
const json = (value: unknown) => JSON.stringify(value, null, 2);

export function ConfirmationWorkspace({ projectId, planId, projectVersionId, scenario = "ready", api = frontendApi }: { projectId?: string; planId?: string; projectVersionId?: string; scenario?: Scenario; api?: FrontendApi }) {
  const [resolvedPlanId, setResolvedPlanId] = useState(planId ?? "");
  const [rounds, setRounds] = useState<ConfirmationRoundView[]>([]);
  const [selected, setSelected] = useState<ConfirmationRoundView | null>(null);
  const [plan, setPlan] = useState<ImplementationPlanView | null>(null);
  const [planVersionId, setPlanVersionId] = useState("");
  const [summary, setSummary] = useState("");
  const [readinessText, setReadinessText] = useState(json(emptyReadiness));
  const [error, setError] = useState("");
  const [message, setMessage] = useState("");
  const [loading, setLoading] = useState(true);
  const [busy, setBusy] = useState(false);
  const [capabilities, setCapabilities] = useState<ProjectCapabilities | null>(null);
  const [detailError, setDetailError] = useState("");
  const canCreateDraft = scenario !== "forbidden" && (capabilities?.canConfirmationCreate === true);
  const canUpdateDraft = scenario !== "forbidden" && (capabilities?.canConfirmationUpdate === true);
  const canConfirm = scenario !== "forbidden" && (capabilities?.canConfirm === true);

  async function load() { setLoading(true); setError(""); setDetailError(""); try { const activePlanId = planId ?? (projectVersionId ? (await api.implementationPlans.list(projectVersionId))[0]?.id : undefined); if (!activePlanId) { setResolvedPlanId(""); setPlan(null); setRounds([]); return; } setResolvedPlanId(activePlanId); const [detail, next] = await Promise.all([api.implementationPlans.get(activePlanId), api.confirmationRounds.list(activePlanId)]); setPlan(detail); setRounds(next); setSelected((current) => next.find((round) => round.id === current?.id) ?? next.at(-1) ?? null); if (projectId) { const overview = await api.projects.overview(projectId, projectVersionId); setCapabilities(overview.capabilities ?? null); if (!overview.capabilities) setDetailError("当前项目未返回可验证成员能力，写操作已安全禁用。"); } } catch (reason) { setError(reason instanceof Error ? reason.message : "确认轮次加载失败，请重试。"); } finally { setLoading(false); } }
  // The async loader synchronizes this client workspace with the selected route context.
  // eslint-disable-next-line react-hooks/set-state-in-effect, react-hooks/exhaustive-deps
  useEffect(() => { void load(); }, [planId, projectId, projectVersionId, api]);
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { if (selected) { setPlanVersionId(selected.planVersionId); setSummary(selected.implementationSummary); setReadinessText(json(selected.readiness)); } }, [selected]);

  function parseReadiness(): ReadinessView { return JSON.parse(readinessText) as ReadinessView; }
  async function create(event: FormEvent) { event.preventDefault(); if (!resolvedPlanId || !canCreateDraft) return; setBusy(true); setError(""); setMessage(""); try { const round = await api.confirmationRounds.create(resolvedPlanId, { planVersionId: planVersionId.trim(), implementationSummary: summary.trim(), readiness: parseReadiness() }); setRounds((old) => [...old, round]); setSelected(round); setMessage("确认草稿已创建；只有 owner 可以最终确认。"); } catch (reason) { setError(reason instanceof Error ? reason.message : "创建失败，当前输入已保留。"); } finally { setBusy(false); } }
  async function update(event: FormEvent) { event.preventDefault(); if (!selected || !canUpdateDraft || selected.status !== "draft") return; setBusy(true); setError(""); setMessage(""); try { const round = await api.confirmationRounds.updateDraft(selected.id, { expectedVersion: selected.rowVersion, planVersionId: planVersionId.trim(), implementationSummary: summary.trim(), readiness: parseReadiness() }); setRounds((old) => old.map((item) => item.id === round.id ? round : item)); setSelected(round); setMessage("草稿已更新；历史已确认内容保持不变。"); } catch (reason) { setError(reason instanceof Error ? reason.message : "更新失败，当前输入已保留。"); } finally { setBusy(false); } }
  async function confirm() { if (!selected || !canConfirm || selected.status !== "draft") return; setBusy(true); setError(""); setMessage(""); try { const confirmed = await api.confirmationRounds.confirm(selected.id, selected.rowVersion); const authoritativeRounds = await api.confirmationRounds.list(resolvedPlanId); const authoritative = authoritativeRounds.find((item) => item.id === confirmed.id) ?? confirmed; setRounds(authoritativeRounds); setSelected(authoritative); setPlan((old) => old ? { ...old, confirmationState: "confirmed" } : old); setMessage("确认轮次已正式确认。"); } catch (reason) { setError(reason instanceof Error ? reason.message : "确认失败，当前输入已保留。"); } finally { setBusy(false); } }
  const draft = rounds.find((round) => round.status === "draft");
  const showCreateDraft = !loading && Boolean(resolvedPlanId) && !draft && (rounds.length === 0 || plan?.confirmationState === "needs_confirmation" || plan?.confirmationState === "needs_reconfirmation");
  const readOnly = !canUpdateDraft || selected?.status !== "draft";

  return <section className="space-y-token-lg"><div><p className="text-sm font-medium text-ai">MVP3 · Confirmation Round</p><h1 className="mt-token-xs text-3xl font-semibold">实现确认工作台</h1><p className="mt-token-xs text-secondary">确认是人类 readiness 事实；不等同于 Test Passed、Release Ready 或 Production Ready。</p></div>{scenario === "forbidden" || capabilities?.readOnly ? <StatusPanel tone="error" title="当前身份只读">你可以查看确认历史，但不能创建、修改或确认。</StatusPanel> : null}{detailError ? <StatusPanel tone="warning" title="权限事实不完整">{detailError}</StatusPanel> : null}{error ? <StatusPanel tone={error.includes("冲突") ? "warning" : "error"} title={error.includes("冲突") ? "并发冲突" : "操作未完成"}>{error} 当前输入没有被清除。</StatusPanel> : null}{message ? <StatusPanel tone="success" title="已完成">{message}</StatusPanel> : null}{!loading && !resolvedPlanId ? <StatusPanel tone="neutral" title="尚无实现计划">请先在当前版本创建实现计划。</StatusPanel> : null}{loading ? <div role="status" className="panel animate-pulse text-muted">正在加载确认历史…</div> : null}{showCreateDraft ? <form className="panel space-y-token-md" onSubmit={create}><h2 className="font-semibold">创建确认草稿</h2><p className="text-sm text-secondary">{rounds.length ? "已有确认历史；当前没有草稿，可创建下一轮并保留来源链。" : "确认草稿只记录人类 readiness 事实。"}</p><label className="block"><span className="field-label">Plan Version ID</span><input className="input-field" value={planVersionId} onChange={(event) => setPlanVersionId(event.target.value)} required /></label><label className="block"><span className="field-label">实现范围摘要</span><textarea className="textarea-field min-h-28" value={summary} onChange={(event) => setSummary(event.target.value)} required minLength={20} maxLength={8000} /></label><label className="block"><span className="field-label">Readiness JSON</span><textarea className="textarea-field min-h-64 font-mono text-xs" value={readinessText} onChange={(event) => setReadinessText(event.target.value)} /></label><Button type="submit" loading={busy} disabled={!canCreateDraft}>创建确认草稿</Button></form> : null}{!loading && rounds.length ? <div className="grid gap-token-lg lg:grid-cols-[16rem_1fr]"><aside className="panel"><h2 className="font-semibold">确认历史</h2><ol className="mt-token-md space-y-token-xs">{rounds.map((round) => <li key={round.id}><button type="button" className={`w-full rounded-token-md p-token-sm text-left ${selected?.id === round.id ? "bg-primary-subtle" : "hover:bg-subtle"}`} onClick={() => setSelected(round)}><strong>Round {round.roundNo}</strong><span className="mt-token-xs block text-xs text-muted">{round.status}{round.isEffective ? " · 当前适用" : ""}{round.sourceRoundId ? ` · 来源 ${round.sourceRoundId}` : ""}</span></button></li>)}</ol></aside><div className="space-y-token-lg">{selected ? <><article className="panel"><div className="flex flex-wrap justify-between gap-token-md"><div><h2 className="font-semibold">Round {selected.roundNo}</h2><p className="mt-token-xs text-sm text-secondary">{selected.status} · Plan Version {selected.planVersionId}</p></div><span className="rounded-full bg-subtle px-token-sm py-token-xs text-xs">{selected.isEffective ? "当前适用" : "历史只读"}</span></div><p className="mt-token-md whitespace-pre-wrap text-sm">{selected.implementationSummary}</p><pre className="mt-token-md overflow-auto rounded-token-md bg-subtle p-token-sm text-xs">{json(selected.readiness)}</pre></article><form className="panel space-y-token-md" onSubmit={update}><h2 className="font-semibold">{readOnly ? "确认历史（只读）" : "编辑确认草稿"}</h2><label className="block"><span className="field-label">Plan Version ID</span><input className="input-field" value={planVersionId} onChange={(event) => setPlanVersionId(event.target.value)} disabled={readOnly} required /></label><label className="block"><span className="field-label">实现范围摘要</span><textarea className="textarea-field min-h-28" value={summary} onChange={(event) => setSummary(event.target.value)} disabled={readOnly} required minLength={20} maxLength={8000} /></label><label className="block"><span className="field-label">Readiness JSON</span><textarea className="textarea-field min-h-64 font-mono text-xs" value={readinessText} onChange={(event) => setReadinessText(event.target.value)} disabled={readOnly} /></label><div className="flex flex-wrap gap-token-sm"><Button type="submit" loading={busy} disabled={readOnly}>保存草稿</Button><Button type="button" variant="secondary" loading={busy} onClick={confirm} disabled={!canConfirm || readOnly}>Owner 最终确认</Button></div></form></> : null}</div></div> : null}</section>;
}
